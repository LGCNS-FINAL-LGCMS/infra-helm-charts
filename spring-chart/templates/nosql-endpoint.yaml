{{ if .Values.nosql.enable }}
apiVersion: v1
kind: Endpoints
{{ with .Values.nosql }}
metadata:
  name: {{ .name }}
subsets:
  - addresses:
      - ip: {{ .ip }}
    ports:
      - port: {{ .port }}
{{ end }}
{{ end }}
