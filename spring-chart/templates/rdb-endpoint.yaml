{{ if .Values.rdb.enable }}
apiVersion: v1
kind: Endpoints
{{ with .Values.rdb }}
metadata:
  name: {{ .name }}
subsets:
  - addresses:
      - ip: {{ .ip }}
    ports:
      - port: {{ .port }}
{{ end }}
{{ end }}